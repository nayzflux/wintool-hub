/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "127.0.0.1",
            "dev.api.wintool.nayz.fr"
        ]
    }
}

module.exports = nextConfig
